{
  "name": "agent-mesh",
  "version": "0.1.0",
  "description": "Local message broker for inter-agent communication via MCP",
  "type": "module",
  "scripts": {
    "broker": "tsx src/broker/index.ts",
    "cli": "tsx bin/cli.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "uuid": "^11.0.0",
    "ws": "^8.18.0",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "@types/ws": "^8.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.4"
  }
}
